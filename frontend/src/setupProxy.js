/**
 * setupProxy.js — CRA Development Proxy
 * Legacy weather-proxy handlers removed.
 */
module.exports = function(app) {
  // All weather proxy requests have been migrated to the conformed backend weather service.
};

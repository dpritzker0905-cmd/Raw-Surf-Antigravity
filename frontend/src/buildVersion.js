// AUTO-STAMPED at deploy by update-sw-version.js (the same script that stamps the service
// worker's BUILD_VERSION) — the two MUST agree so a stale-bundle session is self-diagnosing.
// 'dev' means a local / un-stamped build (npm start).
export const BUILD_VERSION = 'dev';

/**
 * ToS constants — shared between Auth.js, Settings.js, and TosReacceptanceGate.
 * Bump this version when Terms change to trigger re-acceptance for existing users.
 */
export var CURRENT_TOS_VERSION = '1.0';

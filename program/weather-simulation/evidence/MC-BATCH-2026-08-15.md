# Master Codex remediation batch — 2026-08-15 (second session)

**Base tree** `aa305291` (the concurrent session's pushed HEAD) + this batch's working set.
**Source audit** `MASTER_CODEX_AUDIT_REPORT_1.0.md` (external, OneDrive). Every finding was
reproduced at `c1566c8b` before any edit; MC-03 was reproduced **live on production** during the
session. MC-01's full evidence is its own artifact:
`scientific-validation/MC01-capseam-evidence.md` (+ two probe JSONs + the census pair).

## What changed (files)

| change | files |
|---|---|
| WS-CAN-0072 · MC-01 cap seam, dark | `services/weather_pipeline/surf_height_convention.py` (+`publish_surf_height`, `cap_seam_monotone_enabled`, `describe().cap_seam`), `services/weather_pipeline/surf_transform.py` (single return through the helper; 800→795 LOC), `routes/admin/surf_forecast.py` (`SURF_CAP_SEAM_MONOTONE` row + the stale `SURF_HEIGHT_H110` default corrected `"0"`→`"1"`), `tests/test_surf_cap_seam_monotone.py` (new, 47), `tests/test_surf_height_convention.py` (single-conversion-point guard strengthened to the new chain of custody) |
| WS-CAN-0017 · MC-05 escape hatch | `services/noaa_gfs_{wave,wind,pressure}_fetcher.py` (extract-or-refuse in `=0` mode), `tests/test_grib_range_integrity.py` (18→36) |
| WS-CAN-0073 · MC-03 refusal | `routes/weather.py` (`get_report_calibration`), `tests/test_report_calibration.py` (8→12) |
| WS-CAN-0074 · MC-07 bounds | `services/weather_pipeline/schemas.py` (`ClientDiagnosticReport` bounds), `services/weather_pipeline/route_helpers.py` (`append_diagnostic_line` — moved there after the LOC ratchet blocked `weather.py` at 824/800; now 799/747), `routes/weather.py` (sanitized record, generic 500), `tests/test_client_diagnostics_bounds.py` (new, 7) |
| program docs | task register (+3 rows, 2 cells), `BLOCKERS_AND_DECISIONS.md` (D-4, D-5), gate-status addendum, handoff section, execution-state section, this file |

## Exact commands (local interpreter `python3.exe` 3.14.4 — the standing env caveat applies; CI is authority)

```
# red-first, per change (representative)
python -m pytest tests/test_surf_cap_seam_monotone.py -q          # 44F/3P pre → 47P post
python -m pytest tests/test_grib_range_integrity.py -q            # 12F/24P pre → 36P post
python -m pytest tests/test_report_calibration.py -q              # 2F/10P pre → 12P post
python -m pytest tests/test_client_diagnostics_bounds.py -q       # 5F/2P pre → 7P post

# adjacency sweeps (all green post): test_surf_height_convention, test_height_anchors,
# test_owner_calibration_anchors, test_flag_lane_parity, test_measure_or_refuse_last_two_surfaces,
# test_noaa_multi_resolution, test_noaa_fetch_soft_deadline, test_fetcher_http_pooling

# MC-01 census + probe reruns
python program/weather-simulation/evidence/scientific-validation/mc01_capseam_census.py > mc01-capseam-census.json
SURF_CAP_SEAM_MONOTONE=1 python "<OneDrive>/audit_tools/jacobian_probe.py" > mc01-jacobian-probe-repaired.json

# full lanes (results appended below when complete)
bash run_lanes.sh   # pytest $(scripts/ci_test_lanes.py --lane {guards|chain|estate} | tr -d '\r') [+ new untracked files]
```

## Mutation matrices (Edit-tool mutations; unmutated control re-run + `grep -c MUTATION` = 0 after each)

- **Cap seam**: M1 dead switch → monotone + band-theorem + registry tests red · M2 double
  conversion → band-theorem red (monotone alone is blind, by design) · M3 dropped conversion →
  band-theorem red · M4 inert kill switch → flag-off golden red.
- **Range hatch**: MA slice replaced with whole body → wave EXTRACTS red (other fetchers green =
  per-file reach) · MB GRIB-magic check dropped → boundary-refusal red · full revert = the
  red-first run (12F).
- **Diagnostics**: MD2 rotation neutered → rotation test red · schema/leak reverts = the red-first
  run (5F).
- **Calibration**: disable-direction = the red-first run (2F).

## Limitations, stated

- Local interpreter is 3.14 vs declared 3.12; every count above is a THIS-environment reading.
  CI on origin/dev is the authority — which is also why floors were not raised (D-5).
- The MC-01 census grid is synthetic (the audit probe's); production reach is bounded by the
  0.145% cap-bind rate, and the real-spot winter census belongs to the owner's flip decision.
- MC-07 leaves sink separation, dedupe, and the auth model open (recorded in WS-CAN-0074).
- The range hatch cannot validate the LAST `.idx` message of a file: its range is open-ended
  (`bytes=<start>-`), so no requested length exists to compare (deliberate, tested exemption —
  and flagged independently by the concurrent session the same day). A future strengthening
  could bound it via the file's total size; not this visit.
- The concurrent session's uncommitted `lane-estate-run.txt` and the two `forecast_cache/*.json`
  files were left untouched and uncommitted throughout.

## Rollback

Each change is one commit, revertible independently (`git revert <sha>`). The cap seam needs no
rollback path in production terms: the flag is OFF until flipped, and the OFF path is pinned
byte-identical (`test_flag_off_is_byte_identical_legacy_including_the_defect`). The hatch change
only alters behavior under `GRIB_RANGE_STRICT=0`, which no environment is known to set.

## Lane results (this tree = `84475022` + the batch working set; local 3.14 interpreter)

- **guards: 1788 passed, 0 failed**, 66 skipped, 1 xfailed (34:53) — exactly 1741 + the 47 new
  cap-seam tests.
- **chain: 790 passed, 0 failed** (16:11) — exactly 786 + the 4 new calibration-refusal tests.
- **estate: 425 passed, 0 failed**, 2,864 skipped (2:48) — includes the 18 hatch + 7 ingress
  tests. First attempt had ONE red: `test_scheduler_dispatch_offload::
  test_a_blocking_sync_job_does_not_starve_the_loop`, a `time.sleep(0.4)`-vs-10 ms-ticker timing
  assertion with no import path from this batch; isolated control 4/4 green and the full-lane
  rerun above is clean — recorded as load flake, not regression.

Floors deliberately unmoved (D-5): the pair-edit happens from CI's origin/dev reading after an
authorized push.

## Push, deploy, and CI receipts (2026-08-15, owner-authorized push)

**D-5's mechanism was corrected at push time** (see the D-5 correction): `check_floor_before_push`
BLOCKED the batch — the staleness lane grades CI's observed count against `MIN_PASSED`+budget, so
the pair had to move BEFORE the push, anchored to the last green run + exact additions. Applied as
`834b1cfe`; pushed `aa305291..834b1cfe` (8 commits incl. the concurrent session's 3 docs).

| check | result |
|---|---|
| Deploy at the artifact | `/api/health` healthy, version embeds `834b1cfe…`, 0× 5xx at boot |
| MC-03 live | `/report-calibration` → `available:false, reason:"no usable calibration evidence: n_matched=0, n_reports=0 (min 1)"` — the same endpoint served `available:true` on identical evidence 12 h earlier |
| MC-07 live | oversized `event_type` POST → **HTTP 422** (rejected at the door; writes nothing) |
| CI on `834b1cfe` | **CI success ×2** — guards **1788 passed** (13:22), chain **790 passed** (7:23), estate **413 passed** (0:49) — all three exactly the predicted floors source; staleness green. Encoding, LOC ×2, Lighthouse ×2, keep-warm: success |
| E2E | 1 failure, classified ENVIRONMENTAL: Mobile Safari 12/12 + Desktop Chrome green on identical specs; Desktop Firefox failures all at its exact 1.5 m budget, Desktop Safari uniform ~10.5 s launch-level fast-fails; zero frontend files in the push. The rotating-project signature (the documented refutation of "a browser bug"). Failed jobs re-run against the warm box; result recorded when it lands |

CI's estate reading (413) vs the local read (425) = **+12**, consistent with the L-2 structural
local-high decomposition — the bias model now has a third confirming measurement.

# Captured, but stored with its run rather than here

24 geography cells (8 locations × 3 browser configurations) are in
`../browser-device-tests/coverage-*.json` under `geography`, and tabulated in
`../../MODEL_LAYER_GEOGRAPHY_COVERAGE.csv`. Frames are in `../browser-device-tests/shots-*/geo-*.png`.

All 24 rendered, including the antimeridian (179.6°) and 68°N.

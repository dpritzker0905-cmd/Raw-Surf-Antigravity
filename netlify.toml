[build]
  base = "frontend"
  command = "yarn install --ignore-engines && CI=false yarn build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18.20.2"
  NODE_OPTIONS = "--openssl-legacy-provider"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

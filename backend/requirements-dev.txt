# Test/dev dependencies — 2026-08-01.
#
# WHY THIS FILE DID NOT EXIST UNTIL NOW: no pytest ran in GitHub Actions at all, so nothing ever
# had to declare what the suite needs. The 4,727-test backend suite was runnable only on a machine
# where these had been installed by hand at some point, which is not a reproducible state and is
# indistinguishable from "the tests pass" right up until someone checks out the repo clean.
#
# Discovered by wiring the composition guard suite to CI and reading the collection errors:
#   fastmcp    weather_sim_mcp.py and services/weather_pipeline/sim_boot.py import it at module
#              level. Six guard modules failed to import without it.
#   aiosqlite  backend/database.py and services/weather_pipeline/spot_ratings.py import it — the
#              SQLite lane used by tests and local dev. Production runs Postgres via asyncpg, which
#              is why requirements.txt never needed it and why its absence was invisible.
#
# ⚠️ These are deliberately NOT in requirements.txt: that file is what every Render deploy
# installs, and neither is needed to serve traffic. Keep the split.
#
# Pinned to the versions the 2026-08-01 baseline was measured with (44 modules, 737 collected,
# 673 passed, 64 skipped, 0 failed).
-r requirements.txt

pytest==9.0.3
pytest-asyncio==1.3.0
fastmcp==3.3.1
aiosqlite==0.22.1
